<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.SelfAccounting"
        tools:targetApi="31">

        <activity
            android:name=".ui.alarmPage.AlarmActivity"
            android:exported="false" />
        <activity
            android:name=".ui.debtPage.DebtActivity"
            android:exported="false" />
        <activity
            android:name=".ui.budgetPage.BudgetActivity"
            android:exported="false" />
        <activity
            android:name=".ui.homeScreen.MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.subscriptionPage.SubscriptionActivity"
            android:exported="false" />
        <activity
            android:name=".ui.homeScreen.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>